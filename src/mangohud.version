# in base
{
  global:
    overlay_GetInstanceProcAddr;
    overlay_GetDeviceProcAddr;
    overlay_SetMetrics;
    glX*;
    egl*;
    dlsym;
    mangohud_find_glx_ptr;
    mangohud_find_egl_ptr;
  local: *;
};
